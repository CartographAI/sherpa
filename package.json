{
  "name": "@cartographai/sherpa",
  "version": "0.0.1",
  "type": "module",
  "main": "./dist/index.cjs",
  "bin": {
    "sherpa": "dist/index.cjs"
  },
  "files": ["dist"],
  "scripts": {
    "build": "bun run clean && bun run build:web && bun run build:server",
    "build:web": "cd web && bun install && bun run build",
    "build:server": "cd server && bun install && bun run build",
    "clean": "rm -rf dist",
    "dev": "concurrently \"cd web && bun run dev\" \"cd server && bun run dev\"",
    "start": "bun dist/index.js"
  },
  "dependencies": {
    "@ai-sdk/anthropic": "^1.1.0",
    "@modelcontextprotocol/sdk": "^1.3.1",
    "ai": "^4.1.0",
    "hono": "^4.6.17"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "tsup": "^8.3.5",
    "typescript": "^5.7.3"
  }
}
